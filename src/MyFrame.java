import javax.swing.*;
import java.awt.*;

public class MyFrame extends JFrame {
    public MyFrame() {
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setSize(new Dimension(500, 1038));
        setResizable(true);
        setLocationRelativeTo(null);
        setVisible(true);
    }
}
